## Running the Stubs Locally

These are scaffolding stubs only. To explore the structure:

```bash
cp .env.example .env
python -m venv venv
source venv/bin/activate
pip install fastapi uvicorn
uvicorn api/stubs/main:app --reload
